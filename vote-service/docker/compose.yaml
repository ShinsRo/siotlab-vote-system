services:
  mysql:
    image: mysql:8.4
    container_name: vote-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: vote
    volumes:
      - vote-mysql-data:/var/lib/mysql

volumes:
  vote-mysql-data:
