spring:
  docker.compose:
    file: docker/compose.yaml
    lifecycle-management: start-only
  datasource:
    url: jdbc:mysql://localhost:3306/vote
    username: root
    password: root

#logging:
#  level:
